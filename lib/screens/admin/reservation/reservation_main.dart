import 'package:flutter/material.dart';

class ReservationMain extends StatelessWidget {
  const ReservationMain({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Reservations', style: TextStyle(fontSize: 24)),
    );
  }
}
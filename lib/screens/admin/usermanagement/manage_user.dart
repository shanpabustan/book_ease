import 'package:flutter/material.dart';

class ManageUser extends StatelessWidget {
  const ManageUser({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Manage Users', style: TextStyle(fontSize: 24)),
    );
  }
}

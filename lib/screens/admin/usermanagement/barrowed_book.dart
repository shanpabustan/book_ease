import 'package:flutter/material.dart';

class BarrowedBook extends StatelessWidget {
  const BarrowedBook({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Barrowed Books', style: TextStyle(fontSize: 24)),
    );
  }
}
